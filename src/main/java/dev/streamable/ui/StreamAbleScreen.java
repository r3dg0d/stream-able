package dev.streamable.ui;

/**
 * Marker for Stream-able's own screens.
 *
 * <p>While one is open (and "Hide Studio from outputs" is on) recordings and
 * streams keep showing the last game frame from before it opened, so settings -
 * including partially revealed stream keys - never reach viewers.</p>
 */
public interface StreamAbleScreen {
}
