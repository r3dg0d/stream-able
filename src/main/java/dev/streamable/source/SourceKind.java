package dev.streamable.source;

/** The kind of content a {@link StreamSource} renders. */
public enum SourceKind {
    /** A live Chromium page rendered off-screen and composited over the game. */
    BROWSER
}
